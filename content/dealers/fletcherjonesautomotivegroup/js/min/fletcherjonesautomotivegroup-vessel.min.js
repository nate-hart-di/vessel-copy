var modfullOffset = 50;
jQuery(document).ready(function (n) {
  n('[rel=popover]').popover({
    html: !0,
    placement: 'bottom',
    content: function () {
      return n('.savingsDetailsList').html();
    },
  });
  var a,
    t = jQuery(window).height();
  n('.dealersnav ul.sub-menu').css('max-height', '400px'),
    t <= 600 && n('.dealersnav ul.sub-menu').css('max-height', '200px'),
    (isSmallMobile = {
      any: function () {
        return n(window).width() < 767;
      },
    }),
    1199 < n(window).width()
      ? (setTimeout(function () {
          skrollr.init({ forceHeight: !1 });
        }, 500),
        (a = n(this).find('.paralax')),
        ($window = n(window)),
        n(window).scroll(function () {
          a.each(function (a) {
            $bgobj = n(this);
            var t = -($window.scrollTop() / $bgobj.data('speed')),
              e = 0;
            $bgobj.data('padding-top') && (e = $bgobj.data('padding-top')), (t += e);
            n(this).attr('id');
            var o = '50% ' + t + 'px';
            $bgobj.css({ backgroundPosition: o });
          });
        }))
      : (jQuery('.animateImage').attr('data-900', '0px'),
        jQuery('.animateImage').attr('data-1500', '0px'),
        jQuery('.cpocontent').attr('data-900', '0px'),
        jQuery('.cpocontent').attr('data-1500', '0px')),
    n('a.overlay-toggle').click(function () {
      n('a.overlay-toggle').removeClass('blue'),
        n('a.overlay-toggle').removeClass('active'),
        n('.toolbar-overlays, #videooverlay').addClass('open'),
        n('#intro-overlay').removeClass('open'),
        n('.targetOverlay').hide(),
        n(this).addClass('active'),
        n('.inside-tabs, .back-button, .close-overlay').fadeIn(),
        n('#' + n(this).attr('target') + '-overlay')
          .fadeIn(1e3)
          .addClass('showTarget');
    }),
    n('.close-overlay').click(function () {
      n('.inside-tabs, .close-overlay, .back-button').hide(),
        n('.toolbar-overlays, #videooverlay').removeClass('open'),
        n('a.overlay-toggle, a.california').removeClass('active'),
        n('a.overlay-toggle').addClass('blue'),
        n('#intro-overlay').addClass('open');
    }),
    n('.find-button').click(function () {
      n('.button-tabs, .targetDiv').hide(),
        n('a.california').addClass('active'),
        n(this).addClass('active'),
        n('.inside-tabs, .back-button').fadeIn(),
        n('#' + n(this).attr('target') + '-tab')
          .fadeIn(1e3)
          .addClass('showTarget');
    }),
    n('.back-button').click(function () {
      n('.back-button').hide(),
        n('#californialocations-overlay').fadeIn(1e3).addClass('showTarget'),
        n('#southcalifornia-overlay, #northcalifornia-overlay').hide();
    }),
    n(function () {
      n('a.scroll').click(function () {
        if (
          location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') &&
          location.hostname == this.hostname
        ) {
          var a = n(this.hash);
          if ((a = a.length ? a : n('[name=' + this.hash.slice(1) + ']')).length)
            return n('html,body').animate({ scrollTop: a.offset().top }, 1e3), !1;
        }
      });
    }),
    n('#desktop-menu-toggle').click(function () {
      'desktopHeaderMenuOpen' == n(this).attr('data-gtm-event')
        ? n(this).attr('data-gtm-event', 'desktopHeaderMenuClose')
        : n(this).attr('data-gtm-event', 'desktopHeaderMenuOpen');
    }),
    n('#desktop-locations-toggle, #mobile-locations-toggle').sidr({ name: 'sidr-desktop-locations', side: 'right' });
});
