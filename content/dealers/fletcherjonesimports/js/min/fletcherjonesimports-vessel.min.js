function vrp_search_page_title_params(e) {
  var i = 0,
    t = 1;
  for (var n in e)
    'type[]' === n && 1 === e[n].length && 'New' === e[n][0] && (i = 1), ('make[]' !== n && 'make' !== n) || (t = 0);
  1 === i && 1 === t && (e['make[]'] = ['Mercedes-Benz']);
}
jQuery(document).ready(function (r) {
  r('.blog .featured-carousel, .category .featured-carousel').owlCarousel({
    loop: !0,
    margin: 10,
    navigation: !0,
    navigationText: [
      '<i class="fa fa-angle-left" aria-hidden="true"></i>',
      '<i class="fa fa-angle-right" aria-hidden="true"></i>',
    ],
    items: 4,
    itemsDesktop: [1200, 3],
    itemsDesktopSmall: [1024, 2],
    itemsTablet: [767, 1],
  }),
    r('[rel=popover]').popover({
      html: !0,
      placement: 'bottom',
      content: function () {
        return r('.savingsDetailsList').html();
      },
    });
  var e = jQuery(window).height();
  r('.dealersnav ul.sub-menu').css('max-height', '400px'),
    e <= 600 && r('.dealersnav ul.sub-menu').css('max-height', '200px'),
    (isSmallMobile = {
      any: function () {
        return r(window).width() < 767;
      },
    }),
    r('[href="#"]').attr('href', 'javascript:void(0)'),
    isSmallMobile.any() &&
      (r('.gallery a.large-img').removeClass('fancybox'),
      r('#gallerycarousel')
        .find('a')
        .removeClass('fancybox')
        .click(function (e) {
          e.preventDefault(),
            r('.gallery').find('a.large-img').attr('href', r(this).attr('href')),
            r('.gallery').find('a.large-img img').attr('src', r(this).attr('href'));
        }),
      r('#gallerycarousel').touchwipe({
        wipeLeft: function () {
          r('#gallerycarousel').jcarousel('next'), jQuery('.carousel').tooltip('close');
        },
        wipeRight: function () {
          r('#gallerycarousel').jcarousel('prev'), jQuery('.carousel').tooltip('close');
        },
      }),
      r('.carousel').tooltip({
        content: 'Swipe Left/Right to Scroll Gallery!',
        position: { my: 'center', at: 'center' },
      })),
    (isVRP = {
      listview: function () {
        return !!r('.vehicle.list-view').length;
      },
      gridview: function () {
        return !!r('.vehicle.grid-view').length;
      },
      any: function () {
        return !!r('#results-page').length;
      },
    }),
    (isVDP = {
      wide1400: function () {
        return !!r('.maincardetails').length;
      },
      hotwheels: function () {
        return !!r('.project-hotwheels').length;
      },
      any: function () {
        return !!r('.vdpModals').length;
      },
    }),
    isVRP.any() &&
      r('body').on('vrp-ready vrp-ajax-complete', function () {
        var e;
        r('.vehicle').each(function (e, i) {
          var t, n, a, o, l;
          0 == r(this).find('.vehicle-price .primary-cta').length &&
            ((t = r('.visible-xs .vehicle').eq(e)),
            (n = r(this).find('.button-bar .primary-cta .button-form')),
            (a = r(this).find('.button-bar .button.finance-link')),
            (o = r(this).find('.vehicle-price')),
            (l = r(t).find('.vehicle-price')),
            r(n).addClass('block green button button-primary').appendTo(r(t).find('.vehicle-price')),
            r(l)
              .append(r(a).removeClass('hidden-all'))
              .find('.price-top')
              .prepend(
                r('.vehicle-title h2 a', t)
                  .clone()
                  .addClass('pull-right')
                  .text('View Details')
                  .css({ fontSize: '16px', fontWeight: 'bold' }),
              ),
            r(o).append(n, a.removeClass('hidden-all')));
        }),
          r(document).width() < 768 &&
            0 == r('.show-filters-button').length &&
            ((e = r('.sorting-options')),
            r(e)
              .removeClass('hidden-xs hidden-phone')
              .append(
                "<a class='button primary-button show-filters-button align-left' style='margin:0'>Filter Results</a>",
              )
              .find('select')
              .css({ width: '40%' }),
            r('.show-filters-button').click(function (e) {
              r('.mobile-tab-1 .mobile-tab-label').trigger('click');
            })),
          1024 < r(document).width() &&
            r('.roadster-btn').vrpTextHover({
              text: '<p>Create your own lease or finance quote including special e-pricing.</p>',
            });
      }),
    r('.specialCoupon .view-disclaimer-link').on('click', function (e) {
      var i = r(this).parents('.specialCoupon').find('.disclaimer');
      r(i).addClass('open');
    }),
    r('.specialCoupon .disclaimer .fa-close').on('click', function (e) {
      r(this).parents('.disclaimer').removeClass('open');
    }),
    r('body').on('homepage-usp-hidden', function () {}),
    r('a.scroll').click(function () {
      if (
        location.pathname.replace(/^\//, '') == this.pathname.replace(/^\//, '') &&
        location.hostname == this.hostname
      ) {
        var e = r(this.hash);
        if ((e = e.length ? e : r('[name=' + this.hash.slice(1) + ']')).length)
          return r('html,body').animate({ scrollTop: e.offset().top }, 1e3), !1;
      }
    }),
    isVDP.hotwheels() &&
      (r('body').on('click', '#ctabox-premium-features .features-link', function (e) {
        r('#factory_options .panel-title a').trigger('click');
      }),
      r('#mini-header').css('display', 'none'),
      r('.details-page-titlewrap .details-page-row').removeClass('no-spacer'),
      r('.cta-caption > p:first').insertAfter('.disclaimer-small > p:first'),
      r('.panel-title a')
        .not('.collapsed')
        .addClass('collapsed')
        .parents('.panel')
        .find('.panel-collapse')
        .removeClass('in'),
      r(window).resize(),
      1024 < r(window).width() &&
        r('.roadster-button').vdpTextHover({
          text: '<p>Create your own lease or finance quote including special e-pricing.</p>',
        })),
    r(window).width() < 768 && (isMobile.iOS() ? r('.fj-drive .android').hide() : r('.fj-drive .iOS').hide()),
    r('#footer, #videobanner, .menu-top').click(function () {
      r.sidr('close', 'mobile-menu');
    }),
    r('#ctabox-premium-features .features-link').click(function () {
      r(this).parent().toggleClass('open');
    }),
    r('body').hasClass('page-vehicle-display-page') &&
      (r('div#inquire-button').insertAfter('.maincta-row.ctabox-row'),
      r('div#inquire-button a').css('display', 'block')),
    0 == r('#count-number').html() && r('#vehicle-count-overview-number').hide(),
    jQuery('body').on('vrp-ajax-complete vrp-ready', function () {
      var e = r('.vehicle');
      r(e).each(function () {
        var e = r(this).find('.lease-for-less'),
          i = r(this).find('.button-bar-item.hidden-always a');
        r(e).click(function (e) {
          r(i).click();
        }),
          r(this).find('.inquire-cta').appendTo(r(this).find('.vehicle-price'));
      });
    }),
    r('#vehicleDetails .shopping-tool.button.cta-button.inquire').insertAfter('.advanced-pricing-stack'),
    (window.sr = ScrollReveal()),
    document.body.classList.contains('home') &&
      ((window.sr = ScrollReveal({
        duration: 1e3,
        scale: 1,
        origin: 'bottom',
        distance: '150px',
        easing: 'cubic-bezier(0.5, 1.10, 0.4, 1.19)',
        viewFactor: 0.65,
        mobile: !1,
      })),
      sr.reveal('.fly-in-left', { mobile: !1, origin: 'left', duration: 700, viewFactor: 0.65, distance: '150px' }),
      sr.reveal('.fly-in-right', { mobile: !1, origin: 'right', duration: 700, viewFactor: 0.65, distance: '150px' }),
      sr.reveal('.fly-in-bottom', {
        mobile: !1,
        origin: 'bottom',
        duration: 700,
        viewFactor: 0.65,
        distance: '150px',
      })),
    r('.lazy-loading').each(function (e) {
      var i = r(this);
      setTimeout(function () {
        i.addClass('active');
      }, 350 * e);
    });
});
